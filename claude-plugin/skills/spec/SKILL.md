---
name: spec
description: Turn an approved Business Central requirement and architecture into one implementable AL specification. Own technical contracts and acceptance criteria without writing AL implementation or changing architecture.
argument-hint: Requirement name, LOW/MEDIUM/HIGH, and approved architecture or assigned bounded unit
---

# /aldc:spec

<!-- Generated by scripts/sync-plugin-support.js from the al-spec-agent contract; edit ENTRY_SKILLS in the generator, not this file. -->

Read `${CLAUDE_PLUGIN_ROOT}/agents/al-spec-agent.md` in full and act as that agent for the rest of this session. The Copilot-to-Claude Code mappings at the top of that file are binding. This skill changes session instructions, not host permissions; for an enforced agent tool list launch `claude --agent aldc:al-spec-agent` and verify its effective tools.

Input: **$ARGUMENTS**

Requirement artifacts live under `.claude/plans/` in the project; run `/aldc:al-initialize` once if that folder does not exist.
