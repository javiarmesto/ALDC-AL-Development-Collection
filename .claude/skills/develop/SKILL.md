---
name: develop
description: AL Developer - Tactical implementation specialist for Business Central extensions. Edits AL, builds via the terminal, and validates with tests. Implements features following specifications without making architectural decisions.
argument-hint: Implementation task, bug fix, or feature to code (e.g., "Add email validation field to Customer table")
---

# /aldc:develop

<!-- Generated by scripts/sync-plugin-support.js from the al-developer contract; edit ENTRY_SKILLS in the generator, not this file. -->

Read `${CLAUDE_PROJECT_DIR}/claude-plugin/agents/al-developer.md` in full and act as that agent for the rest of this session. The Copilot-to-Claude Code mappings at the top of that file are binding.

Input: **$ARGUMENTS**

Requirement artifacts live under `.claude/plans/` in the project; run `/aldc:al-initialize` once if that folder does not exist.
